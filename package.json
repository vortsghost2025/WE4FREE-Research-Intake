{
  "name": "we4free-research-intake",
  "version": "0.2.0",
  "description": "Research Ingestion + Evidence Scoring + Autonomous Patch Suggestion lane",
  "main": "dist/cli/research-intake.js",
  "scripts": {
    "prebuild": "node -e \"const fs=require('fs');try{fs.rmSync('dist',{recursive:true})}catch{}\"",
    "build": "tsc",
    "start": "node dist/cli/research-intake.js",
    "dev": "ts-node src/cli/research-intake.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:single": "vitest run"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.3.0",
    "commander": "^12.0.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@we4free/ontology": "file:WE4FREE-Ontology",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "vitest": "^4.1.6"
  }
}
