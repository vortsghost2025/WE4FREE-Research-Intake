# Execution Plan — WE4FREE Research Intake

> Session: agent_98237d57 | Stages: Phase A, B, C, D + design3 work orders
> Created: 2026-05-17

---

## What I found

The source tree shows **Phases A–D are substantially implemented**:
- 6 ingest adapters (arXiv, GitHub, OSF, OpenAlex, Semantic Scholar, web-search)
- Full canonicalization layer (claim extraction, evidence linking, authority links, contradictions)
- Graph store + JSONL persistence + Lattice-Deck export
- Multi-signal similarity scoring (keyword overlap, authority, novelty, risk)
- Graph-aware confidence scoring (confidence-scorer.ts)
- HMAC-SHA256 signed packet signing + verification
- Human-review gate with 6 conditions
- Deduplication (SHA256 content hashing, persistent seen-id tracking)
- Quarantine JSONL writer with per-packet report
- CLI with 14 Commander commands
- 6 work orders in `work-orders/` for design3 features (P0–P3 priority)
- 41 tests across 5 modules, all passing (AUDIT.md)
- TypeScript strict: zero compilation errors

---

## Claim / Evidence / Non-claim note

**What the current state demonstrates:**
- The pipeline compiles with strict TS, runs 14 CLI commands, and has 41 passing tests.
- Authored artifacts: the first reviewer showed this numerically (41/41 test pass).
- Scoring, quarantine, and gate logic are wired end-to-end with real file I/O.

**What it does NOT demonstrate:**
- Live API ingestion against real sources (network smoke test not yet run).
- in-toto / WASM / Rego integration — these are design3 features not yet implemented.
- WE4FREE-Ontology extraction — types remain in `src/types.ts`.

---

## Remaining work — 6 design3 work orders (P0 → P2 priority)

Per `AUDIT.md` and the design doc, the highest-value remaining work is:

| Priority | Work Order | Per AUDIT |
|----------|-----------|-----------|
| **P0** | WO-01: in-toto attestation packet format | Hardens safety perimeter |
| **P0** | WO-02: Rego quarantine policy gate | Machine-enforceable quarantine |
| **P1** | WO-03: LangGraph workflow spike | Checkpointing + crash recovery |
| **P1** | WO-05: WE4FREE-Ontology extraction | Prevents schema drift |
| **P2** | WO-04: Scoring upgrade (Paper Scout + ClaimTrust) | Replaces linear trust math |
| **P2** | WO-06: Sparse contradiction detection | Scales beyond ~100 claims |

---

## Execution strategy

### Phase 1 (P0 — security perimeter hardening)

**WO-01: in-toto attestation (WO-01 file + sign-packet.ts)**

1. Read `src/types.ts`, `src/analyze/sign-packet.ts`, `src/analyze/verify-packet.ts`, `src/cli/research-intake.ts`
2. Add `IntotoAttestation` envelope type to `src/types.ts` with `predicateType`, `subject[]` (name + digest), `predicate` fields
3. Modify `sign-packet.ts` — add `format: 'hmac' | 'in-toto'` param; when `'in-toto'`, wrap in envelope instead of inline HMAC
4. Modify `verify-packet.ts` — add detection + verification path for in-toto format
5. Add `--format <hmac|in-toto>` flag to `run` and `daemon` commands in `research-intake.ts`
6. Check for existing tests in `__tests__/sign-verify-packet.test.ts`

**WO-02: Rego quarantine policy (WO-02 file + quarantine.ts)**

1. Check if `@open-policy-agent/opa-wasm` is in package.json (it is not per AUDIT — dependency add required)
2. Create `policies/quarantine-policy.rego` encoding: risk==high → deny; confidence<0.5 → deny; source_url=='' → deny; requires_human_review → flag
3. Add `PolicyResult` type to `src/types.ts`
4. Add optional rego evaluation step to `src/output/quarantine.ts` when policy file is present, graceful fallback when absent
5. `npx tsc --noEmit`

---

### Phase 2 (P1 — workflow durability + ontology stability)

**WO-03: LangGraph workflow spike (WO-03 file)**

1. Add `@langchain/langgraph` to `package.json` — confirm it is absent
2. Create `src/daemon/workflow.ts` — `IntakeState` interface, 6 nodes: discover, normalize, canonicalize, score, quarantine, sign, report; nodewraps existing module functions
3. Quarantine node uses `Command.RESUME` interrupt
4. Add `--workflow` flag to CLI
5. Create `src/daemon/workflow.test.ts`
6. `npx tsc --noEmit`

**WO-05: WE4FREE-Ontology extraction (WO-05 file)**

1. Research `ts-json-schema-generator` viability for generating `.schema.json` from `src/types.ts`
2. Create WE4FREE-Ontory directory structure: `spec/artifacts/`, `spec/graph/`, `spec/packets/`, `spec/scoring/`, `spec/lanes/`
3. Each schema file exports both TypeScript type + JSON Schema object
4. Move canonical type definitions to new package, replace in-project imports
5. `npm run build` with imported types

---

### Phase 3 (P2 — scoring depth + contradiction scale)

**WO-04: Scoring upgrade (WO-04 file)**

1. Read `src/analyze/similarity.ts` and `src/analyze/trust-score.ts` (already read)
2. Add `compositeScore`, `communityActivity`, `recency` to `ScoredArtifact` in `src/types.ts`
3. Replace `computeSimilarity` with Paper Scout 5-signal weighted composite (default: embedding=0.45, keyword=0.15, authority=0.20, community=0.10, recency=0.10) — configurable via `SCORE_WEIGHT_*` env vars
4. Replace linear trust delta math in `trust-score.ts` with PageRank-style iterative propagation
5. Integrate composite score into `confidence-scorer.ts`
6. Add `SCORE_WEIGHT_*` vars to `.env.example`

**WO-06: Sparse contradiction (WO-06 file)**

1. Read `src/analyze/contradiction-detect.ts`
2. Create `src/analyze/sparse-embed.ts` — bag-of-concepts tokenization + IDF weighting + Hoyer sparsity
3. Replace pairwise O(n²) loop in `contradiction-detect.ts` with top-K sparse retrieval (K=5)
4. Fallback to O(n²) for n<50
5. `__tests__/contradiction-detect.test.ts`

---

## Completion criteria

- All 6 work orders marked in_progress → completed in order
- `npx tsc --noEmit` passes after each work order
- Tests pass with `npm test()`
- No existing behavior regressed
